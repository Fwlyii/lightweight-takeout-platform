# SRS 实现进度（2026-09-03）

本文件对应 `elemopractice-refine-yi/report/result/tjumain.pdf`，记录当前 demo 已落地的可操作能力。

## 已落地

- 订单履约：待支付超过 15 分钟自动取消；已送达超过 24 小时自动确认完成；状态更新使用条件更新，重复扫描不会重复处理。
- 注册与加购校验：顾客注册校验 11 位手机号、8–32 位字母数字密码并防止手机号重复；后端加购会校验商品上架、店铺营业和库存，避免仅依赖前端按钮状态。
- 订单快照与支付状态：订单创建时保存收货人、电话和地址快照，地址簿后续修改不会影响历史订单；支付状态独立记录为待支付、已支付、已退款或已取消。
- 评价闭环：已完成订单可评分和填写 500 字内评价；每个订单只能评价一次；商家可以查看自己店铺的评价并回复一次。
- 评价治理：管理员可查看全部评价并将违规评价标记为隐藏（保留原始记录，顾客端和店铺公开列表不再展示）。
- 资产中心：用户可查看余额、积分、优惠券、会员状态；支持演示充值、领取一次新人券、开通 30 天会员；下单时应用有效会员 95 折，支付页再按订单门槛展示并选择红包。
- 支付红包选择：支付页读取当前用户未使用且未过期的红包，按“满减门槛”过滤并默认选取优惠力度最大的红包；支持改选或暂不使用，优惠金额、积分抵扣上限和最终应付金额实时联动；后端支付时再次校验并在同一事务内原子核销，防止重复使用或越权使用。
- 订单提交幂等：顾客端提交订单时生成 `Idempotency-Key`，后端按“顾客 + 幂等键”建立唯一约束；重复点击或网络重试会返回原订单，不会重复扣库存或生成多笔订单。
- 结算资产：支付页支持模拟支付或钱包支付；可按“每 100 积分抵 1 元、最多抵扣应付金额 20%”进行积分抵扣；钱包和积分均采用条件更新防止余额变负，支付、抵扣和取消回退均写入资产流水。
- 积分：订单完成时按实付金额向顾客累计积分（每 1 元 1 分）。
- 库存：商品维护可售库存；顾客端不展示售罄商品；下单时使用数据库条件更新原子预占，库存不足整单失败；主动取消、支付超时、商家拒单和异常取消会恢复预占库存。
- 商家履约：接单后订单先进入“制作中”，商家点击“确认出餐”后才创建待骑手接单任务，避免跳过制作状态。
- 个人消费分析：资产页展示已完成订单数、累计消费、常去店铺和品类数，统计口径只包含已完成订单且按当前账号隔离。
- 个性化偏好：在“我的 → 偏好与外观”中分别管理页面主题和点餐偏好（辣度、口味、忌口、品类标签），跨会话恢复；智能点餐在未输入关键词时读取偏好，并过滤忌口商品；支持恢复默认。钱包页只承载余额、积分、优惠券、会员和资产流水。
- 资产流水：充值、领券、开通会员、完成订单发积分均写入不可覆盖的流水记录，并提供最近流水查询；取消订单会释放已锁定的优惠券。
- AI 结构化点餐：`GET /api/ai/chat/recommendations` 根据关键词和预算筛选真实在售菜品，返回商品 ID、商家、价格等结构化字段；前端可直接加入购物车，模型文本不参与价格计算。
- 管理统计：`/api/admin/statistics` 支持日期区间，统计总订单、完成、取消、异常和有效营业额；`/statistics/export` 输出 UTF-8 CSV。
- 通知与权限：订单超时会写入用户通知；通知查询和已读接口按当前登录用户绑定，不能通过修改 userId 读取或修改他人消息。
- 视觉入口：用户“我的”分别提供“钱包与优惠”和“偏好与外观”入口；AI 客服页增加“智能点餐”；商家“我的”增加顾客评价回复入口；新增页面沿用蓝白配色并适配 375px 宽度。
- 商家详情页体验：顾客进入店铺后可在“点餐 / 评价 / 商家故事”之间切换；顶部提供“外送 / 自取”服务方式切换，菜单、购物车提示和门店信息会同步展示当前方式，评价页读取真实评价数据。
- 外送/自取结算：选择自取时订单落库为 `PICKUP` 且配送费自动为 0；外送订单保存当时配送费，后续商家改价不会影响已创建订单。
- 首页推荐标签规则：商家可配置“是否支持堂食”和一档满减（如满20减3）；后端先按门槛生成候选标签，再按优先级最多展示 3 个，前端只负责渲染。近期购买记录只保留近 30 天且排除取消/异常订单；满减必须满足门槛大于等于 1 元、优惠金额大于 0 且小于门槛；“好评如潮”要求评分不低于 4.7 且展示销量不低于 200；“xx 人购买”要求展示销量不低于 800；组合标签“xx 人爱不释手”还要求评分不低于 4.7 且展示销量不低于 500；堂食、免配送费和低价起送分别读取商家配置。候选标签按“上次买过 > 满减 > 新店开业 > xx 人爱不释手 > 好评如潮 > xx 人购买 > 堂食店 > 免配送费 > 低价起送”排序，默认排序综合考虑个性化购买记录、标签权重、评分、销量和配送优惠，不再使用突出的“销量冠军榜”。
- 商品运营属性：商家添加/编辑商品时可选择菜单分类（招牌推荐、主食、小吃、饮品、套餐）并设置单笔限购数量；商品列表展示分类与限购提示，后端在加购和下单两个环节重复校验，避免绕过前端限制。
- 购物车选中结算：顾客可在同一商家购物车中勾选部分商品分批下单；结算页携带选中商品 ID，后端按选中集合计算金额、预占库存并仅清理已下单商品，未选商品继续保留。

### 首页标签规则表（当前默认配置）

| 标签 | 展示门槛 | 优先级 | 数据来源 |
| --- | --- | ---: | --- |
| 上次买过 | 当前用户近 30 天有该店非取消/非异常订单 | 120 | 用户订单历史（后端） |
| 满 X 减 Y | `X >= 1`、`0 < Y < X` | 105 | 商家满减配置 |
| 新店开业 | 商家创建时间距今不超过 30 天 | 95 | `business.create_time` |
| X 人爱不释手 | 评分 `>= 4.7` 且展示销量 `>= 500` | 88 | 评价均分与展示销量 |
| 好评如潮 | 评分 `>= 4.7` 且展示销量 `>= 200` | 85 | 评价均分与展示销量 |
| X 人购买 | 展示销量 `>= 800`，且未被“爱不释手”覆盖 | 75 | 演示基线 + 真实完成订单 |
| 堂食店 | 商家勾选支持堂食 | 65 | `business.dine_in_available` |
| 免配送费 | 配送费等于 0 | 55 | `business.delivery_price` |
| 低价起送 | 起送价 `<= 20` 元 | 35 | `business.start_price` |

规则引擎会先收集全部满足门槛的候选标签，再按优先级降序截取前 3 个；因此同一个商家可以同时获得多个标签，但不会出现一整行标签挤压卡片。阈值和优先级集中在后端 `BusinessRecommendationPolicy` 中，前端只渲染后端结果，后续可以直接调整，或继续增加“校园配送、深夜营业、回头客”等规则。

## 仍可继续增强

- P0 尚未完全覆盖：严格分页与组合筛选，以及部分管理审核审计字段；当前订单幂等键、支付幂等、库存并发、商品限购和购物车选中结算已通过唯一约束、条件更新和事务保证重复操作不会产生重复副作用。
- P1 尚未完全覆盖：评价图片上传、更细的商家经营趋势和退款/售后流程。
- P2 尚未接入：实时 GPS、语音/图片识别、每日幸运色和 AI 商家摘要，不影响当前核心演示链路。
- 管理端趋势图、异常率的更细分维度可以在现有统计接口上继续扩展。

## 代码已实现、但原 SRS 未明确写出的能力

PDF 说明了业务目标，但没有展开以下实现细节；它们已经存在于当前代码，可在答辩时作为工程化亮点说明：

- 四角色登录入口与路由隔离：顾客、商家、骑手、管理员进入各自工作台，导航不复用不属于该角色的模块。
- WebSocket 实时消息 + 通知表兜底：订单和配送状态会实时推送，连接失败后仍能从通知列表查询。
- 骑手抢单的条件更新/乐观并发控制，以及异常工单的恢复、转派、取消状态机。
- 配送任务接单前的顾客信息脱敏，接单后才展示完整联系方式。
- Docker 一键演示编排、启动/停止脚本和可重复执行的增量数据库迁移。
- AI 客服健康检查、会话历史清理、结构化推荐结果与模型文本事实隔离；调试订单接口仅允许管理员访问。
- 统一蓝白视觉入口、375px 移动端适配、加载/空态/错误提示和商家多店铺管理入口。
- 历史数据兼容与图片兜底：启动迁移时修复旧地址的双重 UTF-8 编码；商家、商品和订单图片加载失败时回退到本地默认图，避免出现乱码地址或破图图标。
- 首页规则后端化：推荐标签门槛、优先级和综合推荐分由 `BusinessRecommendationPolicy` 统一计算；首页、搜索、收藏和商家统计复用 `BusinessSearchColumns` 的评分与销量口径，便于未来更换 Web/小程序前端时保持结果一致。
- 商品分类与限购：新增 `food.category`、`food.purchase_limit` 字段，商家配置与顾客端展示/校验形成闭环。

## 数据库迁移

- 新表：`review`、`user_asset`、`user_coupon`、`user_preference`、`user_asset_ledger`；`food.stock`、`food.category`、`food.purchase_limit`、订单支付字段和收货地址快照字段为增量字段；`business.dine_in_available`、`business.promotion_threshold`、`business.promotion_discount`记录门店展示与满减配置。
- 可重复执行迁移：`elm_bk/srs-enhancements-migration.sql`；同时已合并到全量初始化脚本 `elm_bk/elm_v2.sql`。
