# 安全设计说明

本项目以“前端输入不可信”为基本边界。页面提交的价格、身份、权限和状态只作为请求信息，最终结果由后端依据登录身份与数据库状态重新判断。

## 已落实的防护

- 订单金额由后端读取商品价格、配送规则和优惠资产重新计算；客户端不能指定成交价。
- 下单会校验商家状态、商品上架状态、库存、数量上限、优惠券归属及积分余额，并在事务内完成扣减。
- 账号每次请求都会重新读取数据库中的启用状态和权限；修改密码、停用/删除账号或调整账号信息后，旧令牌不能继续使用。
- 用户、商家、骑手和管理员接口同时在控制器与服务层校验身份和资源归属。
- WebSocket 必须携带有效 JWT，且令牌用户必须与连接路径中的用户一致；通知按用户或角色发送。
- 上传文件按内容签名识别 JPEG、PNG 和 WebP，不信任文件名或请求中的 MIME 类型，并限制为 5 MB。
- AI 回复渲染前先进行 HTML 转义，避免存储型脚本注入。
- JWT 密钥没有代码内置默认值；演示脚本会在本机生成随机密钥，数据库和应用端口默认仅绑定回环地址。
- 跨域来源限制为本机开发地址和课程演示临时域名，不接受任意第三方网页跨域调用。
- 前端认证请求实例只允许访问平台 API 域名，避免绝对 URL 将 JWT 误带到第三方服务。
- 模拟支付、演示充值和免费开通会员均由 `APP_DEMO_ENABLED` 控制，默认关闭。

## 演示环境边界

支付宝和微信选项在课程演示中仅模拟支付结果，不会连接真实资金渠道。真实上线必须使用支付平台的服务端回调验签、金额核对和幂等处理，不能由浏览器通知后端“已经支付”。

演示账号和临时公网地址只适合课堂展示。公开分享结束后应关闭隧道；生产环境还需要独立密钥管理、HTTPS、限流、审计告警、备份恢复和依赖漏洞扫描。
