# 骑手与配送系统——演示说明

## 一、怎么启动

双击项目根目录的“启动演示.command”，然后打开 <http://localhost:18081>。

如果只用命令行：

```bash
docker compose -f docker-compose.demo.yml up -d --build
```

## 二、专用演示账号

所有账号密码都是 `Demo1234!`。

| 角色 | 账号 | 用途 |
|---|---|---|
| 已审核骑手 | `demo_rider` | 上线、抢单、取餐、送达、上报异常 |
| 候选骑手 | `demo_rider_candidate` | 展示“申请审核中”页面 |
| 商家 | `demo_merchant` | 接单后自动创建配送任务 |
| 顾客 | `demo_user` | 查看履约状态并确认收货 |
| 管理员 | `demo_admin` | 审核骑手、处理配送异常 |

## 三、建议答辩演示顺序

1. 用 `demo_rider_candidate` 登录，进入“个人中心 → 申请成为骑手”，展示资质审核状态。
2. 用 `demo_admin` 登录，进入底部“配送调度”，展示申请列表，可选择通过或拒绝。
3. 用 `demo_merchant` 登录，在商家订单页接受“待商家接单”的订单。系统会自动生成配送任务。
4. 用 `demo_rider` 登录，系统会自动跳转到骑手工作台。展示上线开关、收入/里程统计和任务大厅。
5. 接取任务，依次点击“我已到店 → 确认取餐 → 确认送达”。
6. 用 `demo_user` 登录，在订单中点击“确认收货”，完成整条履约链。

异常支线：骑手在进行中任务选择“上报异常”，管理员可在“配送异常工单”中选择恢复配送、重新派单或取消订单。

## 四、这个板块为什么比“多一张骑手表”更扎实

- 订单和配送任务分开建模：订单表示顾客交易，配送任务表示物流履约。
- 抢单使用数据库条件更新，两名骑手同时点击时只有一人成功。
- 未接单前隐藏顾客电话和详细地址，接单后才解锁，体现隐私保护。
- 每次状态变更都要校验前置状态，不能从“已接单”直接跳到“已送达”。
- 所有订单流转写入独立轨迹表，能回答“谁在什么时候为什么改了状态”。
- 异常不是简单弹窗，而是会暂停履约、生成工单，并由管理员闭环处理。
- 配送状态通过 WebSocket 通知相关角色，骑手工作台会自动刷新。

## 五、核心状态链

```text
待支付 → 待商家接单 → 待派单 → 待骑手接单
       → 待取餐 → 配送中 → 已送达 → 已完成
                            └→ 配送异常 → 恢复 / 重新派单 / 取消
```

## 六、主要实现位置

- 前端骑手申请：`elmclient/src/views/RiderApply.vue`
- 前端骑手工作台：`elmclient/src/views/RiderDashboard.vue`
- 前端管理员调度：`elmclient/src/views/AdminRiders.vue`
- 后端履约服务：`elm_bk/src/main/java/com/tju/elm_bk/service/impl/DeliveryServiceImpl.java`
- 骑手服务：`elm_bk/src/main/java/com/tju/elm_bk/service/impl/RiderServiceImpl.java`
- 配送 SQL：`elm_bk/src/main/resources/com/tju/elm_bk/mapper/DeliveryTaskMapper.xml`
- 完整初始化数据库：`elm_bk/elm_v2.sql`
- 旧数据库增量迁移：`elm_bk/rider_delivery_migration.sql`
