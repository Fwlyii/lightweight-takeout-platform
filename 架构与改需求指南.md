# 架构与改需求指南

这份文档不是把每个类都解释一遍，而是说清楚：一条业务规则的权威来源在哪里，老师临时改需求时应该改哪里。

## 一、总原则

- 前端只负责展示、输入和交互，不决定价格、权限、标签资格或订单状态能否跳转。
- Controller 接收 HTTP 参数，Service 组织业务，Policy/Domain Service 保存可独立测试的规则，Mapper 负责数据读写。
- 从请求传入的 `userId`、商品价格、优惠金额和订单总价都不是事实来源；后端必须从 JWT 和数据库重新获取。
- 同一个概念只保留一套规则；页面需要不同文案时，只在前端 presentation 层转换。

## 二、当前的权威入口

| 变化内容 | 主要修改点 | 不应该写在哪里 |
| --- | --- | --- |
| 角色名和后端角色判断 | `AuthorityName` + `CurrentUserService` | 各个 Service 里重复比较 `"ADMIN"` |
| 前端登录端、入口和路由权限 | `src/utils/roles.js` + 路由 `meta.role` | 每个页面各写一套跳转分支 |
| token 和当前用户的本地存储 | `src/utils/auth.js` | 页面直接同时读写 localStorage/sessionStorage |
| 商家评分、销量、人均口径 | `BusinessMapper.xml` 的 `BusinessSearchColumns` | 首页、收藏页、商家统计各写 SQL |
| 首页标签门槛、优先级和综合分 | `BusinessRecommendationPolicy` | Vue 页面根据销量再猜一次标签 |
| 起送价、配送费、商家满减配置 | `BusinessPricingPolicy` | 多个商家接口各自校验 |
| 库存、商家限购和平台 999 上限 | `PurchaseRules` + `ProductPurchasePolicy` | 商品页、购物车和下单各写一套上限 |
| 前端购物车读写 | `src/services/cartService.js` | 页面直接调用旧的 GET 变更接口 |
| 收货地址归属 | JWT + `AddressServiceImpl` + `src/services/addressService.js` | 前端上传 `userId` 或用户名作为归属依据 |
| 点赞和收藏归属 | JWT + `MerchantInteractionServiceImpl` | URL 或请求体中的 `userId` |
| 下单小计、满减、会员折扣、配送费 | `OrderPricingService` | 相信前端提交的 `orderTotal` |
| 创建订单、库存预占、订单明细快照 | `OrderSubmissionService` | Controller 或订单查询方法里顺手写入 |
| 支付、取消退款、完成积分 | `OrderSettlementService` | 订单、配送、定时任务各复制一套资产操作 |
| 外卖/自取语义 | 后端 `FulfillmentMode` | 四处手写字符串和布尔判断 |
| 订单状态的原子跳转与历史 | `OrderStateTransitionService` | 订单和配送 Service 先查后随意 update |
| 配送站内信和实时推送 | `DeliveryNotificationService` | 每个配送动作各组装一次消息 JSON |
| 前端订单/骑手状态文案 | `src/utils/orderPresentation.js` | 订单列表、详情、商家工作台各写 map |
| 金额、评分、日期格式 | `src/utils/formatters.js` | 模板里到处 `Number(...).toFixed(...)` |
| 首页省市区选择和持久化 | `src/composables/useLocationPicker.js` | `Index.vue` 同时维护地图请求和弹窗状态 |

商家展示销量现在由“演示基线数据 + 后续真实完成订单”组成；首页、搜索、收藏和商家统计读取同一 SQL 片段。因此新订单完成后能反映到销量，又不会破坏演示用的丰富基线。

## 三、新人建议阅读顺序

不要从两千行的 Vue 样式或所有 Entity 开始背。先顺着一条真实用例读：

1. `OrderController.orderSubmit`：HTTP 参数从哪里进来。
2. `OrderServiceImpl.orderSubmit`：应用层只做转发和查询协调。
3. `OrderSubmissionService.submit`：身份、商家、地址、购物车、库存和订单快照的完整下单过程。
4. `OrderPricingService.quote`：为什么前端传 1 元也不能篡改订单价格。
5. `OrderStateTransitionService`：状态如何原子推进并留下历史。
6. `OrderSettlementService`：支付、退款、红包、积分和钱包如何保持一致。
7. 对应 Mapper：最后再看每一步如何落到数据库。

订单主链路可以记成：

```text
Controller
  -> OrderServiceImpl（查询与用例编排）
       -> OrderSubmissionService（创建订单）
            -> OrderPricingService（服务端计价）
            -> OrderStateTransitionService（状态与历史）
       -> OrderSettlementService（支付 / 退款 / 完成积分）
```

代码约定：所有 Spring 依赖都使用 `final` 构造器注入；业务类通过 `CurrentUserService` 取登录人；新增规则先寻找上表的权威入口，不要复制到新页面或新 Service。

## 四、改需求示例

### “好评如潮改为 4.8 分且销量 300 才显示”

1. 修改 `BusinessRecommendationPolicy` 的门槛常量。
2. 在 `BusinessRecommendationPolicyTest` 增加刚好不足和刚好达标两个边界。
3. 前端无需修改，它只渲染后端返回的标签。

### “自取也要满 10 元”

1. 在 `OrderPricingService` 修改起送价适用条件。
2. 修改 `OrderPricingServiceTest` 的自取边界用例。
3. 如需前端提前提示，再改页面文案；即使前端没改，后端也会正确拒绝。

### “新增调度员端”

1. 后端在 `AuthorityName` 增加角色，权限接口通过 `CurrentUserService.has(...)` 判断。
2. 前端在 `ROLE_DEFINITIONS` 增加入口和权限名。
3. 工作台路由声明 `meta: { role: 'dispatcher' }`，通用路由守卫自动处理。

### “商家拒单只退钱包，不退积分”

1. 只修改 `OrderSettlementService.cancelOrder`，不要分别改商家拒单、配送异常和超时取消。
2. 修改 `OrderSettlementServiceTest`，明确钱包、积分、红包和库存各自应发生什么。
3. 配送和订单 Service 无需变化，它们只负责决定何时取消。

## 五、必须保留的测试边界

- 客户端把 30 元商品伪造为 1 元，最终价格仍必须按数据库计算。
- 普通用户不能查看别人的收藏、订单或修改别人店铺。
- 配送单必须校验起送价和配送地址；自取单不收配送费，也不进入骑手抢单流程。
- 订单状态必须按允许的前置状态原子更新，不能跳步，不能被并发请求重复接单。
- 首页和收藏页的同一商家必须展示同一评分和销量。

完整后端测试建议在 Java 17 环境执行 `mvn test`；前端执行 `npm run build`；还应在仓库根目录执行 `./scripts/check-architecture.sh`，防止旧购物车接口、魔法状态码和前端伪造身份回流。修改上述权威入口时，应先增加失败用例，再修正实现。

## 六、还存在的技术债

本次先收拢了最容易造成逻辑不一致和安全漏洞的部分，但不意味着项目已经变成理想架构。

- `Index.vue` 的位置状态已经抽出，但样式和商家列表仍偏大；`MerchantBusiness.vue`、`BusinessInfo.vue` 也应继续以“列表、筛选器、编辑弹窗”为边界渐进拆分，不应一次性重写整页。
- 少数旧 Vue 页面已不在路由中使用，暂时保留以兼容历史代码；确认不再回退后可删除。
- `DeliveryServiceImpl` 仍包含较多履约动作；结算和通知已经抽离，后续增加改派、批量调度时应独立成调度用例服务，不要继续堆入该类。
- AI 对话模块仍然较大，后续应把提示词构造、业务工具调用和会话存储拆开；在需求稳定前不要为了行数盲目重写。
- 目前同时存在 `/api/` 和 `/api/v1/` 路径，新接口应优先使用统一版本路径，旧路径只做兼容，不要再增加第三套风格。
