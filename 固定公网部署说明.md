# 固定公网演示部署

## 演示入口

- 固定地址：<https://elm-demo.whliugong.xyz>
- Pages 备用地址：<https://elm-demo.pages.dev>
- Cloudflare Pages 项目：`elm-demo`

严禁把部署项目名改成 `whliugong`。`whliugong.xyz` 和
`www.whliugong.xyz` 是正在运行的主站，不属于本项目。

## 一键更新

在当前 Mac 上双击 `部署固定公网.command`。脚本会依次：

1. 构建并启动最新的 MySQL、Spring Boot 与前端容器；
2. 重建本机后端的 Cloudflare 出口；
3. 构建 Vue 前端；
4. 生成 Pages 同源反向代理；
5. 只向 `elm-demo` 的 `main` 分支发布；
6. 打开固定演示地址。

也可以在仓库根目录执行：

```bash
./scripts/deploy-cloudflare-pages.sh main
```

正式发布前若想先验证而不覆盖固定网站，可用其他分支名，例如：

```bash
./scripts/deploy-cloudflare-pages.sh qa-pages
```

Wrangler 会在输出中给出独立的预览网址。验证无误后再发布 `main`。

## 运行原理与限制

Cloudflare Pages 托管 Vue 的静态文件和一个很薄的反向代理。浏览器访问
`/api`、`/upload`、`/ws` 时，由代理转发到这台 Mac 上的 Spring Boot 服务；
数据库仍然只在本机 Docker 中运行。

因此：

- 发给组员的网址固定不变；
- 前端与接口保持同源，不在代码中写临时域名；
- 演示期间这台 Mac 不能休眠，Docker Desktop 和网络必须保持运行；
- 若页面能打开但业务提示“演示服务暂时离线”，重新双击部署脚本即可刷新后端出口；
- 若需要完全脱离本机长期在线，应另行准备云服务器或 Cloudflare Named
  Tunnel；Pages 本身不能运行 Spring Boot 与 MySQL。
