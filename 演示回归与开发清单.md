# 演示回归与开发清单

这份清单把《实践文档.md》《逐字稿.md》中与本项目直接相关的要求，整理成每次改需求后都可以重复执行的检查表。

## 启动与恢复演示数据

```bash
docker compose -f docker-compose.demo.yml up -d --build
```

演示地址：前端 `http://localhost:18081`，后端 `http://localhost:18080`，MySQL 映射端口 `13306`。

演示账号的密码均为 `Demo1234!`：`demo_user`、`demo_merchant`、`demo_rider`、`demo_rider_candidate`、`demo_admin`。

如果回归测试产生了订单、店铺或账号数据，先导出备份，再执行以下命令恢复干净演示库（会删除项目 Docker 数据卷）：

```bash
docker compose -f docker-compose.demo.yml down -v
docker compose -f docker-compose.demo.yml up -d --build
```

## 四端冒烟流程

1. 顾客端：注册新顾客 → 登录 → 维护收货地址 → 购物车加商品 → 创建订单 → 支付。
   - 若账号有可用红包，在支付页分别验证默认选中、改选和“不使用红包”三种状态，确认底部应付金额同步变化。
   - 连续点击两次“确认下单”或重复发送同一个 `Idempotency-Key`，应只得到同一个订单号。
2. 商家/管理员端：顾客申请商家 → 管理员审核 → 商家申请开店 → 管理员审核 → 商家在工作台维护店铺和商品。
3. 商家/骑手端：商家接单 → 配送任务进入接单广场 → 骑手接单 → 到店 → 取餐 → 送达。
4. 顾客端：确认收货 → 订单变为已完成；骑手端可在“我的订单”查看历史配送。
5. 每个角色退出登录，再分别验证不能访问其他角色的工作台。
6. 异常路径：重复申请、重复接单、越级推进配送状态、未登录访问受保护接口，均应返回明确错误且不改变数据。
7. 偏好与推荐：在“我的 → 偏好与外观”切换浅色/深色并保存，刷新页面后主题保持；点餐偏好与钱包页面分离；首页标签来自后端门槛规则，最多展示 3 个且综合排序稳定。
8. 商品运营属性：商家新增/编辑商品时设置分类和单笔限购；顾客端能看到对应提示，超过限购数量时加购和下单均被后端拒绝。
9. 购物车分批结算：同一商家购物车取消勾选一项，确认结算金额只包含已选商品，提交后未选商品仍留在购物车。

## 角色边界

- 顾客：首页、商家浏览、购物车、订单、地址、收藏、个人中心。
- 商家：经营工作台、订单处理、店铺与菜单入口、账号设置；门店明细不在“我的”重复展示。
- 骑手：接单广场、我的订单、个人中心；不显示顾客地址簿和收藏模块。
- 管理员：首页、用户管理、商铺管理、配送调度；不复用顾客购物流程。

## 改需求时的提交门槛

- 先补一个失败测试（RED），再实现功能使其通过（GREEN），最后再重构。
- 同时覆盖正常、错误、边界、权限四类情况；涉及接口时检查 HTTP 方法、状态码、`/api/v1/` 路径和统一响应格式。
- 后端保持 Entity/Mapper/Service/Controller 分层；前端通过统一 `request` 实例访问 API，页面只负责展示和交互。
- 每次提交只解决一个可说明的问题，提交信息写清变更目的；不要把多天改动一次性压成一个提交。
- AI 生成的代码必须在本地走通，并能在答辩时说明数据流、权限判断和异常处理。

## 当前回归记录

- 已验证顾客注册、商家申请/审核、开店申请/审核、购物车、支付、商家接单、骑手完整配送、顾客确认收货和退出登录。
- 已修复登录身份切换时旧 `localStorage` 覆盖新角色的串端问题。
- 已将商家“我的”收敛为账号信息和“店铺与菜单”入口，门店明细统一在经营工作台维护。
- 已验证同一 `Idempotency-Key` 重复提交返回同一订单号；测试订单随后已标记为逻辑删除，不影响演示数据。
- 已在实际浏览器检查登录页、首页、订单页和资产页；深色主题卡片/输入框/底部导航可见且保持蓝灰配色。
- 2026-09-03 全平台布局复核：统一普通用户页面的全局返回按钮，移除页面内错位/重复返回入口；个人中心仅保留身份中心的商家/骑手入口，地址详情紧跟“收货地址”展开；新增地址、支付、订单列表等固定操作区已在浏览器截图中复核。
- 旧 /orders 入口已统一重定向到 /orderList，避免进入遗留结算页面；骑手从个人中心查看消息时保留 role=rider，继续使用骑手底栏。
- 2026-09-03 主题与排版复核：主题扩展为清爽蓝白、深海夜色、薄荷青、暖阳橙四套方案；偏好页支持即时预览和保存，个人中心改为居中、无位移的响应式布局，避免标题偏移和按钮遮挡。
- 2026-09-03 展示数据整理：补充 8 家演示商家和 15 个商品，设置不同评分、近 30 日销量、堂食/外卖、满减和开店时间属性：首页可同时展示好评、高销量、堂食、满减与新店标签。
- 2026-09-05 架构收拢：登录身份、角色、商家展示指标、首页标签、商家优惠、订单计价与状态跳转分别收拢到共享服务/策略；收藏页与首页复用同一评分和销量 SQL 口径，前端登录存储、角色导航、状态文案和格式化函数统一管理。
- 2026-09-05 自动测试：Java 17 环境下后端 39 项测试全部通过，包括恶意前端伪造价格、支付/退款资产回滚、四套主题、商家满减边界、商品限购、请求身份注入、配送状态、权限和推荐门槛。
- 2026-09-05 第二轮解耦：订单提交、结算、状态流转、配送通知分别成为独立服务；订单主服务由 495 行降至约 234 行；Spring 业务类和 Controller 统一显式构造器注入，首页位置选择抽为独立 composable。
- 2026-09-05 第三轮边界治理：购物车、地址、收藏/点赞均建立前端 service 单一入口；用户身份只从后端 JWT 解析；订单状态统一引用共享常量；删除商品接口失败时伪造演示商品的兜底，并新增 `scripts/check-architecture.sh` 阻止旧接口、魔法状态码和硬编码用户重新进入活跃页面。
- 2026-09-05 遗留清理：移除 10 个没有路由引用、且仍含旧接口/虚拟订单的页面文件，旧网址保留到新工作台的重定向；修复活跃搜索页仍调用上一版 `SearchController` 的问题，搜索与“我的店铺”统一走共享 service。
