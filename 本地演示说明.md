# 去年高分项目：本地演示说明

## 直接访问

- 前端演示：http://localhost:18081
- 后端接口文档：http://localhost:18080/swagger-ui/index.html
- MySQL（仅供调试）：`127.0.0.1:13306`

当前演示由 Docker Desktop 运行。电脑重启后，双击 `启动演示.command` 即可恢复；演示完成后可双击 `停止演示.command`。

## 临时分享给组员

双击 `启动公网分享.command`，系统会通过 Cloudflare Quick Tunnel 生成一个临时的 HTTPS 公网地址。把终端中显示的 `https://...trycloudflare.com` 地址发给组员即可。

这一方案不需要购买服务器、域名或登录 Cloudflare，但有三个限制：

- 本机和 Docker Desktop 必须保持运行；
- 每次重新创建隧道，公网地址可能变化；
- Quick Tunnel 仅用于开发预览，不承诺稳定性，不适合作为最终答辩的唯一部署方式。

双击 `停止公网分享.command` 可关闭公网入口，同时保留本地演示服务。

## 演示账号

所有演示身份使用同一个密码：`Demo1234!`

| 身份 | 用户名 | 主要可看内容 |
| --- | --- | --- |
| 普通用户 | `demo_user` | 首页、商家、菜品、收藏、订单、个人中心、AI 客服界面 |
| 商家 | `demo_merchant` | 商家工作台、店铺/菜单维护、商家订单和评价回复 |
| 骑手 | `demo_rider` | 接单广场、配送中的订单、履约状态更新 |
| 待审核骑手 | `demo_rider_candidate` | 骑手申请审核中的状态页 |
| 管理员 | `demo_admin` | 管理后台、用户管理、商铺管理、审核入口和统计卡片 |

## 命令行方式

在本目录执行：

```bash
docker compose -f docker-compose.demo.yml up -d
```

停止演示：

```bash
docker compose -f docker-compose.demo.yml down
```

查看运行状态：

```bash
docker compose -f docker-compose.demo.yml ps
```

如修改了前后端代码，需要重新构建：

```bash
docker compose -f docker-compose.demo.yml up -d --build
```

## 说明与限制

- 原压缩包的核心代码没有被覆盖；本目录是单独解压出的本地演示副本。
- 原项目没有留下完整的商家、菜品和订单业务数据，因此本地补入了 3 家店、9 个商品和 2 笔订单，目的是让原有界面与业务路径可以直接展示。这些不是学长当时的真实数据。
- 原项目中存在旧服务器地址和明文密钥。本地副本已改为只连接本机数据库，并禁用了原 AI 密钥，避免误连旧服务器或继续使用已经暴露的凭据。
- 为支持公网预览，前端接口和 WebSocket 地址已经改为根据当前访问域名自动选择；Nginx 会在同一域名下转发 Java 后端请求。
- AI 客服的页面、会话记录接口和代码仍在，但真正调用大模型需要以后单独配置合法的 `DEEPSEEK_API_KEY`。当前演示不要依赖 AI 回复成功。
- 商家端原代码存在重复路由命名问题。如“我的 → 切换到商家端”偶尔跳转不正确，可直接访问 http://localhost:18081/merchant/profile 。
- 支付页会自动读取当前账号未使用且未过期的红包；可以点击红包卡片改选，或选择“不使用红包”。订单金额、积分抵扣上限和底部确认金额会同步变化，提交支付时后端会再次校验红包门槛。
- “我的 → 偏好与外观”中的主题选择会立即作用于全局页面；浅色/深色主题均沿用蓝白（蓝灰）配色，设置保存后可跨会话恢复；“钱包与优惠”仅展示余额、积分、优惠券、会员和资产流水。
- 创建订单使用 `POST /api/orders/submit`，前端自动发送 `Idempotency-Key`；重复点击或重试只返回原订单，不重复预占库存。
- 购物车支持勾选部分商品分批结算；本次下单只会清理已选商品，未选商品仍保留在购物车。
- 首页“筛选”支持免配送费、满减、堂食和起送价组合条件；商家较多时可使用“加载更多”逐页浏览，避免一次性堆满页面。
- 商家菜单管理支持商品分类和单笔限购（留空表示不限购）；限购规则在后端加购、下单两个环节校验，顾客端会在商品卡片显示分类和限购提示。
- 商家详情页可切换“外送/自取”；自取订单不收配送费，并在订单中保存履约方式。
