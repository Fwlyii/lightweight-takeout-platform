<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tju.elm_bk.mapper.MerchantInteractionMapper">


    <resultMap id="BaseResultMap" type="com.tju.elm_bk.entity.MerchantInteraction">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="merchant_id" property="merchantId" />
        <result column="liked" property="liked" />
        <result column="collected" property="collected" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 插入互动记录 -->
    <insert id="insert" parameterType="com.tju.elm_bk.entity.MerchantInteraction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO merchant_interaction (user_id, merchant_id, liked, collected, create_time, update_time)
        VALUES (#{userId}, #{merchantId}, #{liked}, #{collected}, NOW(), NOW())
    </insert>

    <!-- 更新互动记录 -->
    <update id="update" parameterType="com.tju.elm_bk.entity.MerchantInteraction">
        UPDATE merchant_interaction
        SET liked = #{liked}, collected = #{collected}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据用户和商家查询互动记录 -->
    <select id="selectByUserAndMerchant" resultMap="BaseResultMap">
        SELECT * FROM merchant_interaction
        WHERE user_id = #{userId} AND merchant_id = #{merchantId}
        LIMIT 1
    </select>

    <!-- 统计商铺的点赞数 -->
    <select id="countLikesByMerchantId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM merchant_interaction
        WHERE merchant_id = #{merchantId} AND liked = true
    </select>

    <!-- 统计商铺的收藏数 -->
    <select id="countCollectionsByMerchantId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM merchant_interaction
        WHERE merchant_id = #{merchantId} AND collected = true
    </select>


</mapper>
